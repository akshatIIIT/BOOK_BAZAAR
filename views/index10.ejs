<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Admin Books</title>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
    }

    .container {
        max-width: 800px;
        margin: 20px auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
        text-align: center;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th, td {
        border-bottom: 1px solid #ddd;
        padding: 10px;
        text-align: left;
    }

    th {
        background-color: #f2f2f2;
    }

    tr:hover {
        background-color: #f9f9f9;
    }

    .input-field {
        width: calc(100% - 22px); /* Adjust width to fit inside container with padding */
        padding: 8px;
        margin: 8px 0;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
    }
    .edit-button {
        padding: 5px 10px;
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    .save-button {
        padding: 5px 10px;
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }
    .edit-button:hover {
        background-color: #45a049;
    }
    .save-button:hover {
        background-color: #45a049;
    }

    .form-container {
        text-align: center;
    }
</style>
</head>
<body>

<div class="container">
    <h1>Admin Books</h1>
    <table>
        <thead>
            <tr>
                <th>Customer Name</th>
                <th>Email</th>
                <th>Contact</th>
                
            </tr>
        </thead>
        <tbody>
            <% books.forEach((row, i) => { %>
                <% row.forEach((element, j) => { %>
                    <tr>
                        <td><%= element.Customer_name %></td>
                        <td><%= element.customer_email%></td>
                        <td><%= element.Customer_contact%></td>
                        
                    </tr>
                <% }); %>
            <% }); %>
            <!-- Add more rows for additional books -->
        </tbody>
    </table>
    
    
</div>

</body>
</html>
